{{#hasportfolios}}
    {{#portfolios}}
        <div class="card mb-2" style="border: 1px solid #d7dfe3;">
            <div class="card-body">
                <h5 class="card-title">{{{name}}}</h5>
                {{#description}}
                    <p class="mb-0">{{{description}}}</p>
                {{/description}}
            </div>
            <div class="card-footer text-muted">
                <a class="btn btn-primary" href="{{{config.wwwroot}}}/mod/evokeportfolio/view.php?id={{{id}}}">{{#str}}view, mod_evokeportfolio{{/str}}</a>
            </div>
        </div>
    {{/portfolios}}
{{/hasportfolios}}
{{^hasportfolios}}
    <div class="alert aler-danger" role="alert">
        <h3>{{#str}}coursenoportfolio, mod_evokeportfolio{{/str}}</h3>
    </div>
{{/hasportfolios}}